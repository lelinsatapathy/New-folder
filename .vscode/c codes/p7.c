#include<stdio.h>

int main(){
    int a=45;
    int b=15;
    char sub1 = maths;
    char sub2 = science;
    printf("the name of subject is:%c",sub1&&sub2);
    scanf("%c",&sub);

    if (sub = maths && science)
    {
        /* code */
        printf("the price is:%d",a);
    }
    else if (sub = maths || science)
    {
        /* code */
        printf("the price is:%d",b);
    }
    
    


    return 0;
}